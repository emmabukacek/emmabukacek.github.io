{
  "name": "brians198.github.io",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:sass && npm run build:static",
    "build:sass": "node_modules/.bin/node-sass src/scss/style.scss dist/style.css",
    "build:static": "cp src/index.* dist/",
    "release": "npm run build && git subtree push --prefix dist origin master"
  },
  "author": "Emma Bukacek",
  "license": "whatever who cares",
  "devDependencies": {
    "node-sass": "~3.8.0"
  }
}
